"use client";

import type { ReactNode } from "react";

type CollectionEntryEditModalProps = {
  children: ReactNode;
};

export default function CollectionEntryEditModal({
  children,
}: CollectionEntryEditModalProps) {
  return <>{children}</>;
}
